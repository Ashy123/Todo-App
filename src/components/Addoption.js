import React from "react";

const Addoption = () => {
  const handlePick = () => {
    alert("ASHY");
  };
  return (
    <div>
      <button onClick={handlePick}>My Name is ASHLEY</button>
    </div>
  );
};

export default Addoption;
